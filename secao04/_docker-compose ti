version: '3.8'
services:
  db:
    image: postgres
    environment:
      POSTGRES_USER=postgres
      POSTGRES_PASSWORD=postgres
    volumes:
      - db:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    expose:
      - "5434"
volumes:
  db:
    driver: local